# LoxleyPack

Thunderstore modpack listing every Loxley mod. Just a manifest — no code. Bump `version_number` and the dependency versions in `package/manifest.json` when a mod releases, then `./build-package.sh`.
